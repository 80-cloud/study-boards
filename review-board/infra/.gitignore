# 機密・環境固有・状態は Git に入れない（SEC-9・事故防止）
terraform.tfvars
*.tfvars
!*.tfvars.example

# Terraform 状態（DB パスワード・JWT secret を含むため最重要）
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# 鍵
*.pem
*.key
